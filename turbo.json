{
  "$schema": "https://turborepo.dev/schema.json",
  "globalDependencies": [".node-version", ".env", "apps/api/.env"],
  "globalPassThroughEnv": ["DATABASE_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": [],
      "env": ["CI", "VITEST_BROWSER_CHANNEL"]
    },
    "test:e2e": {
      "dependsOn": ["^build"],
      "outputs": [],
      "env": [
        "TEST_DATABASE_URL",
        "TEST_INTELLIGENCE_URL",
        "TEST_INTELLIGENCE_INTERNAL_TOKEN",
        "WEB_ORIGIN",
        "SESSION_COOKIE_SECURE"
      ]
    },
    "knip": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  },
  "ui": "tui"
}
